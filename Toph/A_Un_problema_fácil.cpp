
int main()
{
    fastio;
    While(t)
    {
        ll n, x;
        cin >> n >> x;
        vll first(n), second(n);
        for (ll i = 0; i < n; i++)
            cin >> first[i];
        for (ll i = 0; i < n; i++)
            cin >> second[i];

        for(ll i=0;i<n;i++){
            
        }
    }

    return 0;
}
